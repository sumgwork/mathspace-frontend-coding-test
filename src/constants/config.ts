export const INITIAL_HISTOGRAM_SIZE = 20; // This is the max value on the histogram on first load
export const MAX_HISTOGRAM_VALUE = 100; // This is the max value on the histogram
export const MIN_HISTOGRAM_VALUE = 10; // This is the min value on the histogram
export const HORIZONTAL_LINE_INTERVAL = 5; // This controls how far the horizontal line (markers) are to be shown on the graph

export const INITIAL_BAR_VALUES = [10, 7, 4, 14]; // This controls the number of bars and their initial values

export const STEP_SIZE = 1; // how much shall the bar move when dragged
